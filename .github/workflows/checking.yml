name: Checking

on:
  schedule:
    # At 08:00 every day
    - cron: '0 8 * * *'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check
        uses: dawidd6/action-homebrew-bump-formula@livecheck
        with:
          token: ${{secrets.HOMEBREW_GITHUB_API_TOKEN}}
