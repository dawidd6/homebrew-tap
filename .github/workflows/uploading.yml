name: Uploading

on:
  issue_comment:
    types:
      - created

env:
  HOMEBREW_BINTRAY_USER: dawidd6
  HOMEBREW_BINTRAY_KEY: ${{secrets.HOMEBREW_BINTRAY_KEY}}
  NUMBER: ${{github.event.issue.number}}

jobs:
  upload:
    if: >
      contains(github.event.issue.labels.*.name, 'uploaded') == false &&
      github.event.issue.pull_request &&
      github.event.comment.body == '\upload' && (
        github.event.comment.author_association == 'COLLABORATOR' ||
        github.event.comment.author_association == 'MEMBER' ||
        github.event.comment.author_association == 'OWNER'
      )
    name: Upload bottles
    runs-on: ubuntu-latest
    steps:
      - name: Update Homebrew
        run: |
          brew update-reset
      - name: Checkout tap
        uses: actions/checkout@v2
        with:
          token: ${{secrets.HOMEBREW_GITHUB_API_TOKEN}}
      - name: Checkout PR
        uses: dawidd6/action-checkout-pr@v1
        with:
          pr: ${{github.event.issue.number}}
      - name: Setup tap
        run: |
          mkdir -p $(dirname $(brew --repository $GITHUB_REPOSITORY))
          ln -s $PWD $(brew --repository $GITHUB_REPOSITORY)
      - name: Upload bottles
        run: |
          brew pr-pull \
            --tap=$GITHUB_REPOSITORY \
            --bintray-org=$HOMEBREW_BINTRAY_USER \
            --workflow=building.yml \
            --artifact=bottles \
            $NUMBER
      - name: Push commits
        run: |
          git push
